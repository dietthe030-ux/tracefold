# Tracefold pre-lock feasibility evidence

Checked 2026-09-15 against official Studio Dev `studio-dev`, `https://studio-dev.genlayer.com/api`, chain 61997. Selected isolated packages exist and report CLI `0.40.0-rc.3`, GenLayerJS `2.0.0-rc.1`, genlayer-py `0.19.0rc2`, Testing Suite `0.30.0rc2`, GenVM linter `0.11.1rc2`, Transaction Kit `0.1.0-rc.2`. Runner selector: GenVM Manager `v0.6.0-rc5`; recorded universal asset SHA-256 `bd30580f911338d5533460eca8ed714dec371de5803c04e41dbb96430aea7b6e`.

Probe: `docs/preflight/tracefold_feasibility.py`, a no-product-logic contract exercising scalar and `TreeMap` storage, string ABI, view/write, nondeterministic web call, current return wrapper, custom validator and deterministic post-consensus mutation.

With `GENVM_VERSION=v0.6.0-rc5` and `PYTHONUTF8=1`, isolated `genvm-lint 0.11.1rc2 check` returned `Lint passed (3 checks)`, `Validation passed`, contract `TracefoldFeasibility`, two public methods. The first invocation without `PYTHONUTF8=1` failed only while the Windows console encoded the linter's Unicode cross; rerunning with UTF-8 exposed the actual source finding. `run_nondet_default` then failed linter reachability. Current public GenLayer docs retrieved 2026-09-15 document `run_nondet_unsafe`; changing only that wrapper made lint/validation pass. Production will use the exact lint-proven wrapper unless the runtime resolves to a different documented compatible family before CODE_EDIT.

Live read-only `getContractSchemaForCode` through GenLayerJS 2.0.0-rc.1 reached Studio Dev but returned `invalid_contract runner absent` for both the Tracefold probe and the previously live-proven diagnostic contract. This demonstrates a current external Studio runner/reset readiness fault rather than a Tracefold-only schema defect. No write or deployment was attempted. Local validation is PASS; live schema transport is BLOCKED_EXTERNAL until the canonical runner is restored. The plan therefore does not claim full runtime readiness and requires this exact read-only check to pass before technical source lock/PRE_DEPLOY. The failure also activates the Studio Dev reset policy and invalidates reliance on earlier live diagnostic state.

Official references: https://docs.genlayer.com/developers/intelligent-contracts/deploying/network-configuration, https://docs.genlayer.com/developers/intelligent-contracts/features/non-determinism, https://docs.genlayer.com/developers/intelligent-contracts/crafting-prompts, https://docs.genlayer.com/api-references/genlayer-js/contracts.
