# Tracefold milestone impact matrix

Task: tracefold-2026-09-15. Status: PLAN_PENDING_REVIEW. One integrated, independent-project upgrade, expanded by explicit user exception beyond the default same-Task Milestones model.

| Chosen upgrade | Before → after and benefit | Dependencies / blast radius | Gate, tests and live evidence |
|---|---|---|---|
| Current network/runtime and fresh deployment | Studionet 61999 + old SDK/header → Studio Dev 61997 with exact compatible RC stack, fee estimate and fresh source/address | Contract syntax/header, CLI/Python/JS, wallet chain selector, frontend config, signing, fee, all receipts | Official docs/version check, schema/lint/Direct tests, `STUDIO.TOOL_EXECUTION`, `PRE_DEPLOY`, fresh deploy, finalized semantic/consensus/readback and source parity; old evidence excluded |
| Decision evidence and assessment history | Latest-result-only record → bounded append-only per-proposal assessment entries with source revision/status/fingerprint, consequence bands and reason; clear public views | Contract storage/API, nondeterministic validator, frontend tracker, paging/RPC | Prompt-injection, missing/contradictory source, fingerprint consistency, validator-disagreement and cap tests; live distinct/same/unresolved cases; source-to-result readback |
| Objection consequence | Bounded objection record with unclear decision effect → explicit objection status/explanation and guarded reassessment policy without silently reversing finalized clusters | Proposal state, assessment guard, UI and documentation | State-machine tests for objection before/after assessment, replay/caps and rollback; live material transition and readback |
| Cluster conflict handling | Two-cluster proposal rejection → a separate reviewable conflict state with no automatic destructive merge; keep old conservative mutation guard | Cluster index/storage/API, assessment, explorer | Tests for competing clusters, no mutation without consensus-authorized condition, independent readback; do not introduce automatic cluster union unless a separate feasibility and material-plan approval proves safety |
| Recovery and fee/finality UX | Historical long poll/storage intent → RC-compatible fee quote, single-flight intent/hash reconciliation, finalized + semantic success + method readback, clear retry/error states | SDK/client, wallet sessions, transaction UI, local persistence | Wallet-provider/session tests, fee and receipt-shape regression, RPC budget matrix/measured evidence, browser-wallet Vercel E2E |
| Public product and judge evidence | Internal diagnostics-heavy experience → accessible responsive proposal/decision/history/cluster/incident journeys, concise public Docs and reproducible release manifest | Frontend components, README, GitHub, Vercel assets | Typecheck/build, accessibility/responsive review, GitHub presentation, live asset/config parity, final judge journey, `POST_MILESTONE_REVIEW` and separate Milestones form |

Downstream route: `MILESTONE_PLAN_APPROVAL` before source edits; contract schema/runtime and nondeterministic-source gates during implementation; `PRE_DEPLOY` before any Studio write/deploy; `POST_DEPLOY_TEST` after Studio proof and with Vercel E2E plan; GitHub/Vercel exact targets and browser E2E; `POST_MILESTONE_REVIEW` for same-revision dual approval. Each important action uses current Read → Issue → Check and exact feature declarations. E2E plans bind the implementation revision immediately before live action.

Excluded: old-state storage migration (fresh contract); old contract/address/receipts and old GitHub/Vercel/approval inheritance (different Task and chain); Explorer submission (Milestones final form applies); automatic union of existing clusters (unsafe until separate proof); unrelated extra advisory sources/integrations and speculative economics (no demonstrated benefit or runtime feasibility); repeating every wallet journey live (provider variations are automated unless a distinct failure appears). Video is optional. Release-gate exclusions require a recorded dependency boundary at the exact final revision, not a convenience assertion.
