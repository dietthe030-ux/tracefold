# Tracefold replacement PRE_DEPLOY package

Checkpoint: `PRE_DEPLOY` for the corrected independent Tracefold contract. Package ID `TRACEFOLD-E02-PREDEPLOY-V2`. Source baseline Git revision `22c883c6b3a8c1edc3d9d1643018bf46f38498b7`; the exact reviewed candidate revision is the later clean Git commit containing this package and the replacement matrix, verified by the reviewer against HEAD and artifact hashes. Contract source `contracts/tracefold.py`, 50,471 bytes, SHA-256 `C0ECFC464BC9DBAAAFC8A371B8B7E9218FE4696339F6F7754B6FBCA886542CF0`. GenVM header `v0.3.0`; dependency `py-genlayer:5jycge4q8k23462jtb0b9fyey1s9qz928sz2nbrd9mg4sxqg2qng`; source is UPGRADABLE with an explicit constructor/upgrader actor.

## Live trigger and exact correction

First deployment (`df837d5`, old Tracefold candidate only) passed `FINALIZED`, exact source parity and constructor readback at `0xD99Bf40623A7554256F18168C64B482777f10237`. E01 proposal passed with authoritative state. E02 assessment of the official `GHSA-jfh8-c2jp-5v3q` / `CVE-2021-44228` alias finalized with `FINISHED_WITH_RETURN` and `MAJORITY_AGREE`, but stored `DISTINCT` / `KEPT_SEPARATE` with no cluster. This is a product failure, retained in `evidence/studio/E02-assessment-FAIL.json` (SHA-256 `A83185EC58DFD89CDAB031823DA0186AD2452390BB9F04163DF6E68D3648A5AA`). Official GHSA declares the CVE alias while listing five Maven package coordinates inside the same advisory. Previous source falsely made any package-set size above one `UNRELATED` and demoted a model SAME claim to DISTINCT.

Correction in the new source: package coordinates are compared per authoritative publisher. A single advisory with several affected packages is UNKNOWN, never an unrelated-vulnerability proof. Two publishers with an exact shared coordinate are overlapping/exact as appropriate; disjoint package sets without any deterministic alias/link can still be UNRELATED. Disjoint packages with an explicit published alias are UNKNOWN, so the package mapper does not erase stronger identity evidence. The SAME gate still requires two successful exact records, a deterministic alias/link, a bounded model decision and independent validator exact agreement. Objections still block cluster mutation. The forged cross-ecosystem SAME test remains rejected. Full scope/impact/invalidation: `docs/preflight/E02-CORRECTION-PLAN.md` (SHA-256 `A0B33D80FC87A9E320BC43ED4416CECEBB28B924D3608EDC012BC3ACD4D13DC0`) and `evidence/studio/E02-LIVE-INVALIDATION.md`.

## Exact checks and inventory

- Contract lint: 3 checks PASS; `evidence/studio/correction-contract-lint.txt`, SHA-256 `D379390E5ABAE902199E9ECCFDF50CD97D1BAD02EED75237DBF8937843030490`.
- Contract Direct Mode: 26/26 PASS; `evidence/studio/correction-contract-tests.txt`, SHA-256 `9EBB1C120C1694A6C104D2B622EBC0A5F142E4D62B987E0C7611B2350AA28DD1`. Includes the exact five-package one-advisory alias regression, forged unrelated cross-source claim, validator disagreement, all objections, bounded retry/consumption, upgrade authorization and source failure cases. Exact test source SHA-256 `5472BB3FD972E4C6334A403661DB83E45482F98783A6A7336278E6BAF269EC3D`.
- Live read-only `gen_getContractSchemaForCode` on current Studio Dev RPC: constructor and 20 intended public methods (14 views, 6 writes) PASS; `evidence/studio/correction-live-schema.json`, SHA-256 `3B8EFAF47FDDD3A2019F03125F32053674976DF12CB96418550F75AEDB987137`.
- Frontend transaction/wallet/readback code unchanged by this contract-only correction; current rerun: 77/77 tests, TypeScript and production build PASS. The live-contract address remains unwired until a corrected deployment and matrix succeed.
- Storage layout, constructor, public method names/parameters, wallet fee adapter, frontend visual design and network configuration are unchanged. Assessment package band/outcome/fingerprint/history and derived clusters are materially changed and require fresh live evidence.

## Intended new deployment

Target Studio Dev preset `studio-dev`, RPC `https://studio-dev.genlayer.com/api`, chain 61997. Selected actor `actor19`, public address `0x9ec6a971ff91c7540cce6432e7abf7ece005d5a5`; constructor argument is that same address; no linked contracts or value transfer. Official CLI `0.40.0-rc.3`, GenLayerJS `2.0.0-rc.1`, linter `0.11.1rc2`, GenVM Manager `v0.6.0-rc5` were verified against the current toolchain earlier in this Task; recheck readiness and actual balance immediately before a new fee-bearing action. The initial contract remains read-only historical evidence. A corrected replacement needs its own unique operation ID, measured exact fee distribution/value and explicit user fee confirmation. It must reach `FINALIZED`, semantic success, consensus, byte-identical source parity, constructor readback and a fresh full risk-based Studio E2E matrix. The prior E2E batch authorization is target-scoped and cannot be reused for the replacement contract.

The replacement-specific Studio plan is `evidence/studio/E02-REPLACEMENT-E2E-PLAN.md`; the fee authorization request is `docs/preflight/E02-REPLACEMENT-FEE-REQUEST.md`. Address is explicitly pending deployment and no new fee confirmation is inferred from the previous target-scoped E2E reply. The old plan/request remain historical, invalidated artifacts.

Reviewer is asked to decide only `PRE_DEPLOY` for the exact clean HEAD/package containing this file. Do not carry forward `df837d5` approval, live PASS, contract address, fees, release target or POST_DEPLOY_TEST status. Please inspect the live failure, package-coordinate correction, tests, current source, trust boundary and storage/API parity; report all blocking findings and one standalone `ANONYMOUS REVIEW APPROVED - PRE_DEPLOY` or `ANONYMOUS REVIEW CHANGES REQUIRED - PRE_DEPLOY` verdict tied to this revision/package.
