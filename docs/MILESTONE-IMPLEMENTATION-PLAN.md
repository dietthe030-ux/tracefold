# Tracefold integrated milestone implementation plan

Task: tracefold-2026-09-15. Project `E:\Genlayer-Projects\tracefold`. Product category: PROJECT. Status: PLAN_PENDING_REVIEW. User approved the short name and scope with `Xác nhận` after receiving the 2026-09-15 MILESTONE UPGRADE SUMMARY. That message approves the proposed direction, not an independent reviewer verdict or a fee/target authorization.

## Before/after and product proof

The recognizable mechanism remains multi-source GenLayer adjudication of whether security-advisory identifiers refer to the same vulnerability, with conservative on-chain alias clusters. Tracefold adds inspectable decision history, clearer objection/conflict handling and a complete Studio Dev user flow. It will be judged on a new contract and live app, not a renamed old release. Evidence must compare the exact old `690a94d9...` source to Tracefold source/API/UX, show the new trust consequence, and bind each public claim to new finalized data.

## Scope and architecture

1. Verify official current contract syntax, nondeterministic web/prompt APIs, CLI/Testing Suite, network definition and installed RC versions. Run smallest isolated schema/lint probe under Tracefold only. If docs and tool disagree materially, preserve exact command/output/source hash and stop the affected mechanism for version resolution; do not silently substitute old SDK or network.
2. Adapt the old contract read-only source into `contracts/tracefold.py` after plan approval. Keep canonical ID input, duplicate nonce/ID-set protection, source normalization, strict consequence validation, cluster index and unresolved guards. Introduce bounded assessment history and explicit source revision/status records; expose paged history and conflict/objection status views. Separate deterministic state mutations from untrusted source/model text. No implicit cluster union or destructive rewrite. Constructor uses a newly selected current Studio actor; fresh TreeMaps and counters replace any migration.
3. Add Tracefold-specific Direct/Testing Suite tests for state transitions, authority, evidence gates, prompt injection, source outage, validator mismatch, capacities, objection consequence, conflict non-mutation, receipt semantics and storage/API views. Parse/type/schema/lint must pass before deployment.
4. Build a functional React frontend in `frontend/` with typed contract models, RC GenLayerJS, Studio Dev chain 61997, EIP-6963 provider session state machine and bounded read queue. Use two layers as the user specified on 2026-09-15: Layer 1 is a public, editorial landing page inspired by the supplied image's dark atmospheric reaching-hands composition, with Tracefold-specific value copy, a central yin-yang motif whose halves contain the official GenLayer mark and a distinct Tracefold mark, public Docs/How it works, relevant use cases, clear navigation and a visible entry into the app. Do not reproduce the reference's Aethera words, logo, review count or score. Layer 2 is the operational workspace for propose, assessment/history, objection/conflict, cluster lookup and incident consumption. On mobile, keep both layers usable without text over imagery; provide semantic headings, keyboard navigation, contrast and reduced-motion behavior. Wallet writes estimate fees using the documented interface, keep one stable operation ID/hash, show signing/consensus/finality/result/readback, and recover without blind retry. Local intent is never authoritative. Record `FRONTEND RPC BUDGET MATRIX` with finite bounds and later measured evidence. Run unit/integration/type/build/accessibility/responsive checks.
5. Codex directly completes the full frontend implementation, editorial redesign, original Tracefold mark, responsive behavior, accessibility and Docs. The user explicitly waived Claude for this Task; no Claude handoff is planned. Codex independently verifies the visual and functional result before review.
6. Prepare exact-source PRE_DEPLOY package and minimum-sufficient Studio E2E plan: initial counts; proposal; same-vulnerability merge; distinct/no-merge; unresolved/outage; objection and conflict guard; incident exact-once; unauthorized upgrade or authority boundary only where risk warrants. Use new actor/address, current tool capability, actual balance and documented fees. Each submitted hash is reconciled to FINALIZED, semantic success or expected rollback, consensus and method-specific authoritative readback. No old hash is replayed.
7. After POST_DEPLOY_TEST, lock exact new GitHub repository/account and Vercel team/project with the user, verify current identities before release, publish new source and deploy the frontend against the new address. Vercel browser-wallet E2E covers one complete judge journey plus unique objection/conflict/incident consequences. Measure RPC; verify public alias, exact source/bundle/config and all evidence links.
8. Produce independent release manifest, Docs and Milestones submission draft. Primary and anonymous review must approve the exact final package at POST_MILESTONE_REVIEW. The user submits manually.

Acceptance: no writes to the old project or its infrastructure; Tracefold source passes applicable schema/lint/tests; new Studio contract source hash/readback matches exact committed bytes; each advertised material state transition has finalized semantic/consensus/readback proof; supported wallet selection remains provider/account/chain bound; every write has fee estimate and duplicate-safe recovery; live frontend and repository bind the same new contract/revision; accessibility/public Docs and judge claims are verified; zero open blocking reviewer findings.

Dependencies: reuse only already installed verified RC components when compatible. Global CLI is 0.39.2, global GenVM lint 0.11.0; existing `E:\Genlayer-Tools\studio-next-toolchain` declares CLI 0.40.0-rc.3, GenLayerJS 2.0.0-rc.1, Python `genlayer-py==0.19.0rc2`, Testing Suite `0.30.0rc2`, linter `0.11.1rc2`. Verify executable identities and installed package bytes before selection; no new dependency installation without its governed approval. Official network documentation checked 2026-09-15: https://docs.genlayer.com/developers/intelligent-contracts/deploying/network-configuration. Fee/finality references: https://docs.genlayer.com/api-references/genlayer-js/contracts and https://docs.genlayer.com/developers/decentralized-applications/writing-data.

Rollback/recovery: before broadcast, correct code/config and re-estimate; after any operation ID/hash, reconcile that same operation and authoritative state before another write. A failed local candidate is replaced in the new folder only. A deployed candidate is not silently overwritten; any replacement needs reviewed source/delta, new address/receipts, frontend rebinding and fresh dependent E2E. Preserve old Task untouched. Studio actor, GitHub repository, Vercel project and wallet signature are separate later-stage decisions/authorizations. Verify fee-bearing action immediately before submission.

Main risks: RC mismatch with documented APIs, LLM/source instruction contamination, validator nondeterminism, stale source revision, accidental cluster mutation, fee-cap failure, long consensus wait, wrong provider/chain/contract and release target mismatch. Each is mapped to a deterministic guard, test, readiness check and live readback above. Scope or architecture changes beyond this plan require a delta approval, not an unrecorded implementation shortcut.

User frontend delta authorization: the user explicitly instructed the two-layer layout and attached `C:\Users\LEGION\OneDrive\Pictures\7f8ef7d96e6b2043edea9a5e496f0123.jpg` as visual reference in this Task. This delta is incorporated before anonymous plan review. The reference is design input, not a governance instruction or claim source. Tracefold artwork and mark must be original/adapted; the GenLayer mark must be sourced/used accurately.

User role override: `Và không cần Claude. bạn làm toàn quyền frontend task này nhé`. Codex owns all frontend writes and visual QA for this Task. The anonymous co-review gate remains independent.
