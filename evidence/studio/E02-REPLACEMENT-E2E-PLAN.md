# Tracefold corrected-source Studio E2E matrix

Status: PRE_DEPLOY draft for corrected-source replacement; address **PENDING**. This is a new deployment and new evidence lifecycle. Package ID `TRACEFOLD-E02-PREDEPLOY-V2`; source baseline commit `22c883c6b3a8c1edc3d9d1643018bf46f38498b7`, source SHA-256 `C0ECFC464BC9DBAAAFC8A371B8B7E9218FE4696339F6F7754B6FBCA886542CF0`, GenVM v0.3.0. Final candidate commit must contain this plan and be separately reviewer-approved. Target `studio-dev`, chain 61997, `https://studio-dev.genlayer.com/api`. Selected deployer/upgrader/test actor `actor19`, `0x9ec6a971ff91c7540cce6432e7abf7ece005d5a5`. Old address `0xD99Bf40623A7554256F18168C64B482777f10237` and its E01/E02 operations are historical; none is replayed.

Before each fee-bearing case: recheck current actor balance/network/tool readiness; measure the exact branch fee through the installed documented interface; submit returned `distribution` and `feeValue` unchanged; enforce the user-approved per-operation and aggregate deposit ceilings; reserve one new stable operation ID and broadcast once. Observe the same hash with bounded polling. Every positive case requires `FINALIZED`, `FINISHED_WITH_RETURN` (or current documented semantic equivalent), accepted consensus/`MAJORITY_AGREE`, sender/origin matching actor and exact authoritative pre/post readback. Expected rejection requires the exact reason and unchanged authoritative state. If targeted fee simulation rejects before submission, retain it as a read-only negative probe and do **not** falsely count it as a live transaction. All attempted cases, including failures and corrected probes, remain in the ledger.

| Case | Material transition / risk | New operation ID; method and exact non-secret arguments | Required readback |
|---|---|---|---|
| R00 | Fresh deployment identity | `tf2-deploy-c0ecfc4`; constructor upgrader `0x9ec6a971ff91c7540cce6432e7abf7ece005d5a5` | Fresh address, finalized semantic/consensus success, `gen_getContractCode` exact 50,471-byte SHA, `get_upgrader` actor, all counts zero. Deployment authorization is separate from E2E authorization. |
| R01 | New proposal / nonce | `tf2-r01-propose-log4shell`; `propose_alias_set("tf2-log4shell","CVE-2021-44228","GHSA-jfh8-c2jp-5v3q")` | Proposal 1 PROPOSED, exact two IDs, nonce resolves 1, count +1. |
| R02 | **E02 live regression:** multi-package advisory alias | `tf2-r02-assess-log4shell`; `assess_proposal(1)` | Outcome SAME_VULNERABILITY, package relation not UNRELATED, proposal MERGED, cluster 1 exact aliases, count +1, final fingerprint independently reproducible. This criterion is mandatory; a different outcome is FAIL and triggers diagnosis, not a passed substitute. |
| R03 | Independent alias proposal | `tf2-r03-propose-log4j-fix`; `propose_alias_set("tf2-log4j-fix","CVE-2021-45046","GHSA-7rjr-3q55-vv33")` | Proposal 2 PROPOSED, nonce 2, proposal count +1. |
| R04 | Second independently published alias | `tf2-r04-assess-log4j-fix`; `assess_proposal(2)` | SAME/MERGED cluster 2, cluster 1 unchanged. If live source/model rejects SAME, record the actual non-conclusive result and select a different preverified public alias pair through a documented matrix correction before claiming cross-cluster proof. |
| R05 | Cross-cluster conflict guard | `tf2-r05-cross-cluster`; `propose_alias_set("tf2-cross-cluster","CVE-2021-44228","GHSA-7rjr-3q55-vv33")` | Proposal 3 CONFLICT with `[1,2]`; both existing cluster records unchanged; no automatic merge. Depends on R02/R04. |
| R06 | Open proposal for objection | `tf2-r06-propose-objection`; `propose_alias_set("tf2-objection","CVE-2022-22965","GHSA-36p3-wjmg-h94x")` | Proposal 4 PROPOSED with zero objections. |
| R07 | Immutable objection persistence | `tf2-r07-record-objection`; `record_objection(4,"DIFFERENT_ROOT_CAUSE","Live corrected-source objection guard")` | Objection count +1, paged row exact actor/reason/note, proposal still open, no cluster mutation. |
| R08 | Objection blocks published SAME | `tf2-r08-assess-objection`; `assess_proposal(4)` | Published alias is classified SAME absent objection; with objection final outcome UNRESOLVED and `objection_guard_applied=true`, cluster count unchanged, assessment history/fingerprint match. If public source/model produces a different outcome, this specific guard remains unproved and needs a preverified replacement pair; never force a pass. |
| R09 | Unavailable public source proposal | `tf2-r09-propose-unavailable`; `propose_alias_set("tf2-unavailable","CVE-2099-99999999")` | Proposal 5 PROPOSED; no cluster. |
| R10 | Fail closed on unavailable exact record | `tf2-r10-assess-unavailable`; `assess_proposal(5)` | UNRESOLVED with inspectable source statuses/history and no cluster. |
| R11 | Bounded retry cooldown negative | `tf2-r11-retry-cooldown`; `retry_unresolved(5)` immediately | Expected cooldown rejection and unchanged proposal/history/counts. If exact fee simulation rejects before broadcast, retain simulation error and local current-runtime regression evidence; do not broadcast without measured fees or claim Studio transaction PASS. |
| R12 | Distinct published pair proposal | `tf2-r12-propose-distinct`; `propose_alias_set("tf2-distinct","CVE-2021-45046","GHSA-36p3-wjmg-h94x")` | Proposal 6 PROPOSED; exact IDs/nonce. |
| R13 | No merge from unrelated identities | `tf2-r13-assess-distinct`; `assess_proposal(6)` | DISTINCT or RELATED_NOT_SAME / KEPT_SEPARATE, clusters unchanged. If evidence is ambiguous, record UNRESOLVED and keep this criterion open until a preverified replacement pair proves it. |
| R14 | Incident consumption | `tf2-r14-consume`; `consume_incident("tf2-incident-001","1")` | Returns true; consumption count +1; paged receipt binds caller/context/cluster 1. Depends on R02. |
| R15 | Exact-once duplicate negative | `tf2-r15-duplicate-consume`; repeat `consume_incident("tf2-incident-001","1")` | Exact duplicate rejection and original receipt/count unchanged. If exact fee simulation rejects first, record it as an unsent read-only negative probe and retain local current-runtime regression; never bypass fee estimate to spend for a negative case. |

The plan has 15 potential E2E fee-bearing operations plus one deployment. A replacement public pair may add one separately documented operation under a future user-approved E2E ceiling; it is never silently appended. Full source/identity/cap/injection/validator-disagreement permutations already pass in the current-runtime local suite; Studio rows focus on unique live state transitions and source behavior. No live upgrade rehearsal is added because upgrade execution is not an advertised user feature, no linked-contract migration exists and the exact local upgrade lifecycle passes.

After R00, bind the real address and deployed-code parity in a new deployment manifest and regenerate the E2E action preflight/plan binding without mutating this PRE_DEPLOY-reviewed source. After all required rows are proved, request independent `POST_DEPLOY_TEST` approval for the exact source/deployment/evidence package. Frontend address/release claims, GitHub and Vercel gates remain later steps. The user-owned stop boundary is immediately before Vercel browser-wallet E2E.
