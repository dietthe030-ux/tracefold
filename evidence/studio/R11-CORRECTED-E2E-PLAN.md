# Tracefold authoritative-time replacement Studio matrix

Status: PRE_DEPLOY draft. Release address is pending. Candidate source SHA-256 is `2ED3F93EF341BB98CDD288522D1868AE5DC27EFAB633639552E3D1DA7395377F` (50,674 bytes). Target is `studio-dev`, chain 61997, RPC `https://studio-dev.genlayer.com/api`; deployer/upgrader/test actor is `actor19`, address `0x9ec6a971ff91c7540cce6432e7abf7ece005d5a5`. Addresses `0xD99Bf40623A7554256F18168C64B482777f10237` and `0x5E2899A296378a42d2F5542507cac3094bD8b84E` are historical and will receive no further writes. Publisher inputs were checked read-only in `evidence/pre-deploy/r11-source-preverification.json`.

Every write uses an exact tool fee estimate, one new stable `tf3-*` operation ID and one broadcast. The same hash is observed with bounded polling. PASS requires `FINALIZED`, semantic `FINISHED_WITH_RETURN`, successful consensus, matching sender/origin and authoritative readback. Expected rejection requires the exact error and unchanged state. No failed operation ID is replayed.

| Case | Purpose and exact call | Required proof |
|---|---|---|
| T00 | `tf3-deploy-2ed3f93`; deploy exact source with constructor `"0x9ec6a971ff91c7540cce6432e7abf7ece005d5a5"` | Fresh address; exact 50,674-byte source/hash parity; upgrader actor19; all counts zero. |
| T01 | `tf3-t01-propose-alias`; `propose_alias_set("tf3-alias","CVE-2024-26130","","PYSEC-2024-225")` | Proposal 1 PROPOSED, exact IDs/nonce, transaction `created_at` equals the authoritative transaction timestamp. |
| T02 | `tf3-t02-assess-alias`; `assess_proposal(1)` | NVD/OSV 200; SAME_VULNERABILITY; proposal MERGED; cluster 1 exact aliases; assessment `assessed_at` and proposal `last_assessed_at` equal the authoritative transaction timestamp; fingerprint reproduced. |
| T03P | `tf3-t03p-propose-cluster2`; `propose_alias_set("tf3-cluster2","CVE-2023-41335","","PYSEC-2023-185")` | Proposal 2 PROPOSED, exact IDs and nonce. |
| T03A | `tf3-t03a-assess-cluster2`; `assess_proposal(2)` | NVD/OSV 200; SAME/MERGED; cluster 2 exact aliases; cluster 1 byte-equivalent. |
| T04 | `tf3-t04-cross-conflict`; `propose_alias_set("tf3-cross","CVE-2024-26130","","PYSEC-2023-185")` | Proposal 3 CONFLICT with exact `[1,2]`; proposal/cluster counts become 3/2 and both clusters remain byte-equivalent. |
| T05 | `tf3-t05-objection-proposal`; `propose_alias_set("tf3-objection","CVE-2022-40897","","PYSEC-2022-43012")` | Proposal 4 PROPOSED with exact IDs/nonce and zero objections. |
| T06 | `tf3-t06-objection`; `record_objection(4,"DIFFERENT_ROOT_CAUSE","Live authoritative-time objection guard")` | Returns 1; paged row contains actor/reason/note and `objected_at` equals receipt transaction timestamp; proposal remains PROPOSED; counts remain proposal 4/cluster 2/consumption 0. |
| T07 | `tf3-t07-objection-assess`; `assess_proposal(4)` | NVD/OSV 200 and underlying SAME fields; objection guard yields UNRESOLVED, objection_guard_applied true, one history row, attempts 1, reproducible fingerprint; cluster count stays 2. |
| T08 | `tf3-t08-unavailable-proposal`; `propose_alias_set("tf3-unavailable","CVE-2099-99999999")` | Proposal is PROPOSED with no cluster. |
| T09 | `tf3-t09-unavailable-assess`; `assess_proposal(5)` | UNRESOLVED, exact NVD record absent, one history row, attempts 1, no alias resolution/cluster; `last_assessed_at` equals receipt timestamp; counts become proposal 5/cluster 2/consumption 0. |
| T10 | `tf3-t10-cooldown-negative`; immediate fee simulation of `retry_unresolved(5)` | Exact `Retry cooldown active (Ns remaining; cooldown is 600s)` with N in 1..600; exit is pre-broadcast rejection; proposal 5 byte-equivalent, history stays 1, counts stay 5/2/0, no journal/hash. |
| T11 | `tf3-t11-cooldown-expiry`; after receipt timestamp +600 seconds, `retry_unresolved(5)` | Fee estimate succeeds; single broadcast finalizes; status remains honestly source-derived, attempts/history increase exactly from 1 to 2, last_assessed_at equals the new receipt timestamp; counts stay 5/2/0; the 56,895,061-second anomaly is absent. Useful independent cases run during the interval; no polling loop or blind retry. |
| T12 | `tf3-t12-distinct-proposal`; `propose_alias_set("tf3-distinct","CVE-2021-45046","","PYSEC-2022-43012")` | Proposal 6 PROPOSED with exact IDs/nonce; no cluster mutation. NVD resolves CVE-2021-45046 while OSV aliases CVE-2022-40897, so the records are preverified unrelated. |
| T13 | `tf3-t13-distinct-assess`; `assess_proposal(6)` | NVD/OSV 200; DISTINCT or RELATED_NOT_SAME and KEPT_SEPARATE; one history row and reproducible fingerprint; cluster count remains 2. Any UNRESOLVED result keeps this criterion open. |
| T14 | `tf3-t14-consume`; `consume_incident("tf3-incident-001","1")` | True; one caller/context/cluster receipt with `consumed_at` equal receipt timestamp. |
| T15 | `tf3-t15-duplicate-consume`; fee simulation of `consume_incident("tf3-incident-001","1")` | Exact `Incident context already consumed for this caller` pre-broadcast rejection; original consumption row byte-equivalent, consumption count stays 1, no journal/hash. |

The fixed-source live matrix uses preverified NVD/OSV pairs for deterministic availability. The earlier R02 transaction remains historical corroboration that the unchanged GHSA multi-package mapping reached SAME on Studio; the exact local multi-package regression is rerun on this candidate. Neither substitutes for corrected-source time cases T01/T02/T06/T09/T10/T11/T14. Any source/configuration change invalidates this plan and requires another exact-source review.
